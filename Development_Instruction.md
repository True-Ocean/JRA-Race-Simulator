# 開発仕様書：JRA CARD SIMULATOR

**バージョン:** 1.0（改訂版）
**策定:** 2026年4月14日
**実装優先順位:** MVP段階的開発（Phase 1 → 3）

---

## 目次

1. [プロジェクト概要](#1-プロジェクト概要)
2. [アーキテクチャ方針](#2-アーキテクチャ方針)
3. [データ構造（入力JSON）](#3-データ構造入力json)
4. [パラメータ計算ロジック](#4-パラメータ計算ロジック)
5. [セットアップ画面（ユーザー介入UI）](#5-セットアップ画面ユーザー介入ui)
6. [シミュレーション・エンジン](#6-シミュレーションエンジン)
7. [フィールド・座標系の定義](#7-フィールド座標系の定義)
8. [レーン移動AI（自律行動ロジック）](#8-レーン移動ai自律行動ロジック)
9. [バトル解決ロジック](#9-バトル解決ロジック)
10. [ゴール判定と着順確定](#10-ゴール判定と着順確定)
11. [演出・UIアニメーション](#11-演出uiアニメーション)
12. [ログ仕様](#12-ログ仕様)
13. [技術スタック](#13-技術スタック)
14. [MVP段階的実装計画](#14-mvp段階的実装計画)

---

## 1. プロジェクト概要

### コンセプト
競馬レースの展開を「タロットカード型の自律バトル」として抽象化したシミュレーター。出走馬データを物理的な「走り」ではなく、盤面上の**「ポジション争い＋数値の奪い合い」**に変換し、ユーザーの事前調整を経て最終着順を導き出す。

### 盤面ビジュアルの基本方針
- **馬カード:** 縦長カード（縦横比 約1.55:1）。最大18頭が同時に盤面に配置できる小型サイズ。枠色の縁取り＋上部カラー帯で馬番・馬名を1行表示。カードサイズに応じてフォントも自動調整
- **進行方向:** 上向き（画面下=スタート、画面上=ゴール）
- **レーン配置:** 横並び5レーン。右端=最内（Lane 1）、左端=大外（Lane 5）。右回りレースを想定
- **ラチ表現:** 盤面左右端に白い太めのライン（外ラチ・内ラチ）を引き、その外側に深緑の支柱を一定間隔で描画する。レーンラベル（大外/外/中/内/最内）は表示しない
- **盤面背景色:** 芝レースは緑（`hsl(120, 55%, 22%)`ベース）で全フェーズ統一。ダートは茶色。馬場状態（重・不良）は明度を下げ、荒れた芝パッチ（楕円ランダム）を追加描画。コーナーフェーズでは外レーン(3〜5)に黄色の薄いオーバーレイのみ追加
- **フェーズ名:** 各フェーズに日本語のフェーズ名を盤面中央に大きく薄く表示（例：スタート、スタート〜1コーナー手前、向正面、第1コーナー、最終直線 など）
- **カード重なり防止:** 同一レーン内で複数頭が近接する場合、縦方向（進行方向）にカードをずらして重ならないように描画する
- **スタート演出:** ゲートが横並びに表示され、そこから各馬カードが上方向へ動き始める
- **フェーズ制進行:** フェーズごとに盤面を切り替え（前フェーズのポジションを引き継ぐ）。コーナーフェーズは同一レイアウトのまま外レーンに距離ロス警告色を重ねて表現
- **手動進行:** 各フェーズの処理完了後に一時停止し、ユーザーが「次のフェーズ」ボタンを押して手動で進める。最終フェーズの次は「ゴール判定」ボタンに変更
- **3カラムレイアウト:** ブラウザ画面は「出馬表（左）」「盤面（中央・縦長）」「レースログ（右）」の3カラム構成とする

### ゲームループ（全体フロー）

```
[JSONデータ読み込み]
        ↓
[パラメータ計算・正規化]
        ↓
[セットアップ画面] ← ユーザーが10ptを各馬に振り分け、◎〇▲を付与
        ↓
[シミュレーション実行]
  ↗ フェーズ進行（動的生成）
  ↗ レーン移動AI（自律）
  ↗ バトル発生・解決
  ↗ スタミナ・速度の動的更新
        ↓
[最終タイム算出]
        ↓
[ゴール演出 → 着順表示]
```

---

## 2. アーキテクチャ方針

| 項目 | 方針 |
|------|------|
| 状態管理 | シングルトン `RaceState` オブジェクトで全馬の状態を一元管理 |
| 乱数再現性 | `race_id` をシード値とした**シード付き乱数（PRNG）**を使用。同一 `race_id` では何度実行しても同じ結果になる |
| フレームレート | ロジック更新は**60fps（`requestAnimationFrame`）**で処理。UIアニメーションは `Web Animations API` で分離 |
| 責務分離 | ①計算エンジン（純粋関数）②描画エンジン（DOM操作）③UIコントローラー の3層に分離 |

### シード付き乱数の実装例（Mulberry32）

```javascript
function mulberry32(seed) {
  return function() {
    seed |= 0; seed = seed + 0x6D2B79F5 | 0;
    let t = Math.imul(seed ^ seed >>> 15, 1 | seed);
    t = t + Math.imul(t ^ t >>> 7, 61 | t) ^ t;
    return ((t ^ t >>> 14) >>> 0) / 4294967296;
  };
}
// 使用例: const rng = mulberry32(race_id);
// rng() で [0,1) の再現可能な乱数を生成
```

---

## 3. データ構造（入力JSON）

システムは以下の構造を持つJSONから全初期値を算出する。

```json
{
  "race_id": 20250601,
  "race_info": {
    "track": "芝",
    "distance": 2400,
    "condition": "良"
  },
  "entries": [
    {
      "gate": 1,
      "color": "#FFFFFF",
      "horse": {
        "name": "ウマノナマエ",
        "style": "差し",
        "ave_3f": 36.2,
        "last_3f": 33.5,
        "results": [2, 1, 0, 5]
      },
      "jockey": {
        "name": "ルメール",
        "win_rate": 0.25,
        "top3_rate": 0.55
      }
    }
  ]
}
```

**フィールド補足：**
- `race_id`：乱数シードとして使用（必須）
- `results`：着順の配列。`0` は着外（6着以下）を表す
- `ave_3f`：道中の平均3ハロン（小さいほど速い）
- `last_3f`：最終3ハロン（小さいほど速い）

---

## 4. パラメータ計算ロジック

### 4-1. 3パラメータの定義と役割

| パラメータ | 記号 | ベースデータ | 役割 |
|---|---|---|---|
| 巡航速度 | `S_cruise` | `ave_3f` | 道中ポジション維持の強さ |
| 立ち回り | `M_maneuv` | `jockey.win_rate` | バトル勝率・レーン変更成功率 |
| 粘り強さ | `S_sustain` | `results` + `last_3f` | スタミナ総量・直線の伸び |

### 4-2. スコア計算式

計算前に全馬の `ave_3f` と `last_3f` の**最小値（最速）**をまず求める。

**① 巡航速度 (S\_cruise):**
$$S_{cruise} = \left(\frac{\min(\text{全馬の ave\_3f})}{\text{当該馬の ave\_3f}}\right) \times 80 + (\text{ユーザー介入} \times 2)$$

**② 立ち回り (M\_maneuv):**
$$M_{maneuv} = (\text{win\_rate} \times 200) + (\text{ユーザー介入} \times 2) + \text{印補正}$$

- 印補正：◎ = +10、〇 = +5、▲ = +3

**③ 粘り強さ (S\_sustain):**
$$S_{sustain} = \left(\frac{\text{3着以内回数}}{\text{全出走回数}} \times 50\right) + \left(\frac{\min(\text{全馬の last\_3f})}{\text{当該馬の last\_3f}} \times 30\right) + (\text{ユーザー介入} \times 2)$$

### 4-3. スコアの正規化

**全スコアは最終的に [0, 100] に収める：**
```javascript
const normalize = (score) => Math.max(0, Math.min(100, score));
```

### 4-4. スタミナ初期値と馬場係数

**スタミナ初期値：**
$$InitialStamina = S_{sustain} \times 2.0$$

**馬場係数 (`TrackModifier`)：** スタミナ消費に乗算

| 馬場状態 | 芝 | ダート |
|---|---|---|
| 良 | 1.0 | 1.0 |
| 稍重 | 1.1 | 1.05 |
| 重 | 1.2 | 1.1 |
| 不良 | 1.35 | 1.2 |

---

## 5. セットアップ画面（ユーザー介入UI）

### 画面構成

- **カードリスト:** 最大18頭を一覧表示（馬番・馬名・脚質・3パラメータのバー表示）
- **ポイント配分:** 合計**10ポイント**を上限として、各馬のパラメータを ±5pt の範囲で調整
- **印付与:** ◎・〇・▲ をそれぞれ1頭ずつ選択（重複不可）

### バリデーション要件

```
- 合計ポイント消費 ≤ 10pt（超過時はUI側で制御）
- 1パラメータへの最大投入は +5pt まで
- ◎・〇・▲ はそれぞれ1頭のみ（再クリックで解除）
```

---

## 6. シミュレーション・エンジン

### 6-1. フェーズ管理

レースを**複数フェーズ（時間ステップ）**に分割して処理する。

**フェーズ数の決定:**

| 距離 | フェーズ数 | 備考 |
|------|---------|------|
| 1200m | 5 | スプリント |
| 1600m | 7 | マイル |
| 2000m | 8 | 中距離 |
| 2400m | 9 | クラシック |
| 3000m以上 | 11 | 長距離 |

汎用式：`フェーズ数 = Math.round(distance / 270)`（端数は切り捨て後に最低5を保証）

**各フェーズの基準距離:**
$$d_{phase} = \frac{\text{総距離}}{\text{全フェーズ数}}$$

**コーナーフェーズの割り当て（2400mの例）:**

| フェーズ | 区間 | 種別 |
|---------|------|------|
| 1 | スタート〜第1コーナー | 直線 |
| 2 | 第1コーナー | **コーナー** |
| 3 | 第1〜第2コーナー | 直線（バックストレッチ） |
| 4 | 第2コーナー | **コーナー** |
| 5 | 第2〜第3コーナー | 直線 |
| 6 | 第3コーナー | **コーナー** |
| 7 | 第3〜第4コーナー | 直線 |
| 8 | 第4コーナー | **コーナー** |
| 9 | 最終直線 | 直線（ゴール） |

### 6-2. 実効速度の計算

各フェーズ・各馬の移動量は「実効速度」で決まる：

$V_{eff} = S_{cruise} \times StaminaModifier \times BattlePenalty \times PaceMultiplier$

| 係数 | 値 | 条件 |
|---|---|---|
| `StaminaModifier` | 1.0 | スタミナ > 0 |
| `StaminaModifier` | 0.7 | スタミナ枯渇（= 0以下） |
| `BattlePenalty` | 1.0 | 前フェーズでバトル勝利または非発生 |
| `BattlePenalty` | 0.85 | 前フェーズでバトル敗北 |
| `PaceMultiplier` | 脚質×フェーズ比率で決まる動的倍率 | 下表参照 |

**脚質別ペース倍率（`PaceMultiplier`）:**

脚質ごとにフェーズ進行率（0〜1）に応じたペース倍率配列を定義し、序盤・終盤の加速を表現する。

| 脚質 | 序盤 | 中盤 | 終盤 | 特徴 |
|---|---|---|---|---|
| 逃げ | 1.35 | 1.05→0.85 | 0.70 | 序盤爆発、終盤失速 |
| 先行 | 1.15 | 1.05→0.95 | 0.88 | 安定した先行ペース |
| 差し | 0.88 | 0.95→1.10 | 1.25 | 後半加速 |
| 追込 | 0.80 | 0.88→1.10 | 1.40 | 終盤爆発、大外強襲 |

**X座標（進行距離）の更新:**
```javascript
horse.x += V_eff * dt;  // dt = 1フレーム当たりの時間（秒）
```

### 6-3. スタミナ消費

各フェーズ終了時に以下を計算して差し引く：

$$Cons = (d_{phase} \times TrackModifier) + (バトル敗北回数 \times 10) + (コーナー外差し補正)$$

- **コーナー外差し補正:** コーナーフェーズで「外・大外」レーンにいた馬は +5 を加算

スタミナが 0 を下回った場合は 0 にクランプする（負値は不許可）。

### 6-4. コーナーでの距離ロス

コーナーフェーズでは、各馬の**累積ロス距離**を以下で計上する：

$$DistanceLoss += d_{phase} \times (L_{coeff} - 1)$$

**レーン別距離係数 (L\_coeff):**

| レーン | 係数 | 距離増加 |
|---|---|---|
| 最内（Lane 1） | 1.00 | 基準 |
| 内（Lane 2） | 1.00 | 基準 |
| 中（Lane 3） | 1.02 | +2% |
| 外（Lane 4） | 1.05 | +5% |
| 大外（Lane 5） | 1.08 | +8% |

**戦略的トレードオフ（設計意図）:**
- **内枠:** 距離ロスなし。ただし `M_maneuv` が低いと包囲されやすくバトルペナルティが大きくなる
- **外枠:** 距離ロスあり。バトル発生率が極めて低く、`S_cruise` を100%発揮できる

---

## 7. フィールド・座標系の定義

### 7-1. ビジュアル設定

| 属性 | 芝 | ダート |
|---|---|---|
| ベースカラー | `hsl(120, 60%, 35%)` | `hsl(30, 50%, 40%)` |
| 良（彩度） | 60% | 50% |
| 重（彩度・明度） | 40% / 25% | 35% / 30% |

### 7-2. 座標系の数値定義

**X座標（進行方向）：**
- 初期値: `horse.x = 0`
- ゴール: `horse.x >= コース距離`
- 単位: 任意のゲーム単位（描画時にピクセルへ変換）

**Y座標（レーン）：**
- 範囲: `1.0`（最内）〜 `5.0`（大外）の浮動小数
- 初期値: ゲート番号から自動割り当て（ゲート1→y=1.0、以降均等分配）
- 毎フレーム目標レーン（`TargetLane`）へ一定レートで近づける

```javascript
horse.y += (horse.targetLane - horse.y) * LANE_CHANGE_RATE; // LANE_CHANGE_RATE = 0.1 推奨
```

**20%オフセット（並走時の表示重なり防止）：**
```javascript
const displayY = horse.y + (horse.id % 2 === 0 ? 0.2 : -0.2);
```

### 7-3. 荒れ場の配置

- 最内レーン（Lane 1）の**第2コーナー〜第3コーナー区間**に「荒れ場ゾーン」を配置
- 荒れ場ゾーン滞在中：スタミナ消費量を **+20%** 加算（`TrackModifier` に乗算）

---

## 8. レーン移動AI（自律行動ロジック）

各馬は以下の優先順位で `TargetLane` を決定する（毎フェーズ評価）：

### 優先度1：スタミナ温存モード（最優先）
```
スタミナ残量 ≤ InitialStamina × 0.3 の場合
→ TargetLane = 1 または 2（最短距離を優先）
```

### 優先度2：荒れ場回避
```
現在地の前方1フェーズ以内に荒れ場ゾーンが存在する場合
→ TargetLane = 現在Lane + 1（外へ回避）
```

### 優先度3：前方障害物回避
```
同レーン・前方1マス以内に他馬がいる場合
→ TargetLane = 現在Lane + 1（外へ回避）
   ただし Lane = 5（大外）の場合は減速して後方待機（V_eff × 0.9）
```

### 優先度4：脚質ベース行動（デフォルト）
```
「逃げ」 → 序盤に一気に最内(Lane 1)へ。その後も内を維持
「先行」 → 序盤は Lane 2 を確保。中盤以降も内寄りを維持
「差し」 → 序盤は Lane 3〜4 の中団外。終盤（ratio > 0.65）で Lane 2 へ切り込む
「追込」 → 序盤〜中盤は Lane 4〜5 の後方外目で待機。最終フェーズで Lane 3〜4 から末脚
```

> **注意:** 最終直線フェーズ（最後の1フェーズ）では、全脚質の回避判定をオフにし、直線加速に専念させる。
> **設計意図:** 脚質ペース倍率（`PaceMultiplier`）と組み合わせることで、逃げ馬の序盤逃走・差し追込馬の後半外強襲が**パラメータから自律的に**導出され、毎回異なる展開になる。

---

## 9. バトル解決ロジック

### 9-1. バトル発生条件

```
以下を両方満たす場合にバトル判定を行う：
① 馬AとBのX座標差が閾値以内（同一フェーズ内に接触）かつ同レーン内
② 以下の確率ロールに成功した場合のみバトル発生
```

**バトル発生確率:**
$$P(battle) = 0.5 + CrowdFactor$$

- `CrowdFactor`: 同レーン前後1マス以内に3頭以上密集する場合 +0.2（それ以外 0.0）

### 9-2. バトル処理の制限

- **同一フェーズ内で複数バトルが発生した場合:** 最も前方にいるペア1組のみ**カットイン演出**を発生させる。その他は盤面上の光エフェクトのみ。
- **処理順序:** 同一フェーズ内のバトル判定は「現在X座標が大きい（前方の）馬」から順番に処理する。

### 9-3. バトル勝敗判定

**期待値の計算:**
$$E = M_{maneuv} \times 0.6 + S_{cruise} \times 0.4 + rng(-5, 5)$$

`rng(-5, 5)` はシード付き乱数で生成。

**勝敗判定:** `E_A > E_B` ならA勝利、それ以外はB勝利。

**ペナルティ（敗者に適用）:**
- 次フェーズの `BattlePenalty = 0.85`（速度15%低下）
- スタミナを **5** 消費

---

## 10. ゴール判定と着順確定

### 10-1. 最終タイムの算出

全フェーズ終了後、各馬の到達タイムを一括計算する：

$$ArrivalTime = \frac{(コース距離 + DistanceLoss累計)}{V_{eff\_final} \times (1 + StaminaBonus)}$$

| 変数 | 説明 |
|---|---|
| `DistanceLoss累計` | コーナー外走りによる追加距離の合計 |
| `V_eff_final` | 最終フェーズの実効速度 |
| `StaminaBonus` | 最終スタミナ残量 / `InitialStamina` × 0.1（最大+10%の補正） |

### 10-2. 着順の確定

`ArrivalTime` の昇順にソートして 1着〜18着を確定する。

---

## 11. 演出・UIアニメーション

### 11-1. バトルカットイン演出

- 盤面のアニメーション速度を**30%**にスローダウン（`Web Animations API` の `playbackRate = 0.3`）
- 対象の2枚のカードを前面にズームアップ表示（`z-index` 上位 + `transform: scale(1.5)`）
- 勝敗確定後、通常速度に復帰

### 11-2. ゴール通過アニメーション

1. 画面を**横スクロール**モードに切り替え
2. 全18枚を左端（`horse.x = 0`）に整列
3. 各馬の `ArrivalTime` を正規化し、`transition-duration` に変換して**一斉スタート**

```javascript
// ArrivalTimeを [MIN_DURATION, MAX_DURATION] にマッピング
const minTime = Math.min(...arrivalTimes);
const maxTime = Math.max(...arrivalTimes);
const duration = (arrivalTime - minTime) / (maxTime - minTime) * SPREAD_MS + BASE_MS;
// BASE_MS = 3000ms, SPREAD_MS = 2000ms を推奨
card.style.transitionDuration = `${duration}ms`;
```

4. ゴールライン通過順に着順を確定し、**上位3頭のカードを反転（正位置）**させて表示

### 11-3. レスポンシブ対応

| 状態 | レイアウト |
|---|---|
| シミュレーション中 | スマホ縦向き（ポートレート） |
| ゴール演出 | 横向き（ランドスケープ）へスムーズ切り替え |

`Screen Orientation API` または CSS メディアクエリ＋スクロール制御で対応。

---

## 12. ログ仕様

### フォーマット

```
[Phase {n}][Horse {id}:{馬名}] {Action} (Reason: {理由})
[Battle] Horse {idA} vs Horse {idB} → Winner: {id} ({説明})
```

### 出力例

```
[Phase 3][Horse 5:アパパネ] Lane 3 → 2 (Reason: 内枠先行狙い)
[Phase 3][Horse 7:ゴールドシップ] 荒れ場回避 Lane 1 → 2 (Reason: スタミナ保護)
[Battle] Horse 5 vs Horse 7 → Winner: 5 (E: 72.3 vs 68.1, 競り合い勝利)
[Phase 7][Horse 12:ディープインパクト] スタミナ枯渇 速度-30% (Stamina: 0)
```

### 表示仕様

- サイドパネルまたは折りたたみ式のリストとして表示
- 直近20件を常時表示（過去ログはスクロール参照可）
- バトルログは**強調表示**（色分けなど）

---

## 13. 技術スタック

| 項目 | 推奨技術 | 理由 |
|---|---|---|
| アニメーション制御 | `Web Animations API` | スローモーション（playbackRate）制御が可能 |
| フレーム管理 | `requestAnimationFrame` | 60fps でのロジック更新 |
| 乱数 | Mulberry32（シード付きPRNG） | 再現性の保証 |
| データフォーマット | JSON | 入力仕様の標準化 |
| スタイリング | CSS Custom Properties | 馬場状態による動的な色変更 |
| UIフレームワーク | **なし推奨（バニラJS）** / Vue.js可 | 依存を最小化してデバッグ容易に |

**外部ライブラリ使用の判断基準:**
- ライブラリを導入する場合、バンドルサイズを考慮し `<50KB` を目安とする
- アニメーション目的での jQuery / GSAP 導入は非推奨（Web Animations API で代替可能）

---

## 14. MVP段階的実装計画

### Phase 1（最小実装・デバッグ基盤）
**目標:** ロジックの正常動作確認

- [ ] JSONパーサーとパラメータ計算
- [ ] 5頭立て・直線コースのみ
- [ ] バトルなし（速度ベースのX座標更新のみ）
- [ ] コンソールへのログ出力
- [ ] 最終タイム算出と着順表示（テキストのみ）

### Phase 2（コア機能・ビジュアル）
**目標:** 18頭・フル盤面の動作

- [ ] 18頭対応
- [ ] 5レーン縦長盤面のビジュアル描画（右=最内、左=大外、上向き進行）
- [ ] 馬カードを縦長デザインで描画（18頭対応サイズ）
- [ ] スタート時のゲート横並び表示
- [ ] フェーズ完了後の一時停止＋手動進行ボタン（「次のフェーズ」）
- [ ] 脚質別ペース倍率（`PaceMultiplier`）によるフェーズごとの速度変動
- [ ] レーン移動AI（脚質×フェーズ比率による自律行動）
- [ ] コーナーフェーズの背景色切り替えとレーン距離ロス計算
- [ ] スタミナ動的更新
- [ ] サイドパネルへのログ出力

### Phase 3（演出・ユーザー体験）
**目標:** プロダクト品質

- [ ] セットアップ画面（ポイント配分UI・印付与）
- [ ] バトルカットイン演出（スローモーション）
- [ ] ゴールアニメーション（横スクロール）
- [ ] 荒れ場ゾーンの視覚化
- [ ] レスポンシブ対応（縦→横切り替え）
- [ ] 結果画面（着順・払い戻し表示）

---

## 付録：主要定数一覧

```javascript
const CONFIG = {
  LANE_COUNT: 5,              // レーン数
  LANE_CHANGE_RATE: 0.15,    // 毎フレームのレーン移動速度
  BATTLE_BASE_RATE: 0.5,     // バトル発生基本確率
  BATTLE_CROWD_BONUS: 0.2,   // 密集時のバトル確率加算
  STAMINA_CRITICAL: 0.3,     // スタミナ温存モード発動閾値（残30%）
  STAMINA_MODIFIER_EMPTY: 0.7, // スタミナ枯渇時の速度係数
  BATTLE_PENALTY: 0.85,      // バトル敗北時の速度係数（次フェーズ）
  BATTLE_STAMINA_COST: 5,    // バトル敗北時のスタミナ消費
  CORNER_STAMINA_COST: 5,    // コーナー外走り時の追加スタミナ消費
  ROUGHZONE_STAMINA_MULT: 1.2, // 荒れ場ゾーンのスタミナ消費倍率
  GOAL_BASE_DURATION_MS: 3000, // ゴールアニメーション最速タイム(ms)
  GOAL_SPREAD_MS: 2000,       // ゴールアニメーション最大タイム差(ms)
  DISPLAY_OFFSET_Y: 0.2,     // 並走表示用Yオフセット

  // 脚質別フェーズペース倍率（フェーズ比率 0→1 に対応する倍率配列）
  STYLE_PACE: {
    '逃げ':  [1.35, 1.20, 1.05, 0.95, 0.85, 0.80, 0.75, 0.70, 0.68],
    '先行':  [1.15, 1.12, 1.10, 1.05, 1.00, 0.95, 0.90, 0.88, 0.88],
    '差し':  [0.88, 0.90, 0.92, 0.98, 1.05, 1.12, 1.18, 1.22, 1.25],
    '追込':  [0.80, 0.82, 0.84, 0.88, 0.95, 1.10, 1.22, 1.35, 1.40],
  },
};
```
