{
  "name": "jra-race-simulator",
  "private": true,
  "type": "module",
  "scripts": {
    "preview": "python3 -m http.server 8080 --bind 127.0.0.1",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
